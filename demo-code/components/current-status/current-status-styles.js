export const styles = `
    :host {
        display: block;
        margin: var(--size-3) 0;
    }

    .current-status {
        font-family: var(--font-sans);
    }

    h2 {
        color: var(--text-1);
        font-size: var(--font-size-3);
        margin: 0 0 var(--size-2) 0;
    }

    .status {
        font-size: var(--font-size-2);
        margin: 0;
    }
`; 
